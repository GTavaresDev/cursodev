export function Footer() {
  return (
    <footer className="border-t border-[rgb(var(--color-border))]">
      <div className="mx-auto max-w-listing px-4 py-8 text-sm text-muted">
        Blog construido sobre os endpoints do projeto.
      </div>
    </footer>
  );
}
